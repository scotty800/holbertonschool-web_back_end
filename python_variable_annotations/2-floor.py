#!/usr/bin/env python3
"""Returns the floor of a floating-point number."""


import math


def floor(n: float) -> int:
    """Returns the floor of a floating-point number."""
    return math.floor(n)
